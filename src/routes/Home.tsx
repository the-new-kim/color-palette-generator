import Palette from "../components/Palette";

function Home() {
  return (
    <section className="relative overflow-hidden w-screen min-h-screen flex bg-slate-400">
      <Palette />
    </section>
  );
}

export default Home;
